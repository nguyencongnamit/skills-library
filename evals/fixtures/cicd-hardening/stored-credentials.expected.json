{
  "schema_version": "1.0",
  "last_updated": "2026-05-15",
  "fixture": "stored-credentials.yml",
  "description": "Fixture authenticates to AWS via long-lived `secrets.AWS_ACCESS_KEY_ID` / `secrets.AWS_SECRET_ACCESS_KEY` instead of OIDC. Two findings are expected: gha-oidc-cloud-credentials (the secrets.* reference) and gha-default-permissions-read (no top-level permissions block).",
  "expected_findings": [
    {"rule": "gha-default-permissions-read", "ecosystem": "github-actions", "source": "skills/cicd-security/checklists/github_actions_hardening.yaml"},
    {"rule": "gha-oidc-cloud-credentials", "ecosystem": "github-actions", "source": "skills/cicd-security/checklists/github_actions_hardening.yaml"}
  ]
}
